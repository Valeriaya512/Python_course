## 純註解半成品(依註解寫程式)

請依照下方每一行註解的說明,寫出對應的 Python 程式碼。

```
# 步驟 1:匯入 numpy,並取別名 np

# 步驟 2:匯入 matplotlib.pyplot,並取別名 plt

# 步驟 3:用 np.arange() 產生 0 到 4(不含 4)、間隔 0.2 的陣列,存入 x

# 步驟 4:計算 y1 = cos(pi * x)(用 np.cos 與 np.pi)

# 步驟 5:計算 y2 = cos(2 * pi * x)

# 步驟 6:用 subplot 把版面切成 1 列 2 欄,選第 1 格(左圖)

# 步驟 7:畫出 x 與 y1,使用綠色實線

# 步驟 8:用 subplot 選第 2 格(右圖)

# 步驟 9:畫出 x 與 y2,使用洋紅色點線

# 步驟 10:顯示圖形
```



## 範例prompt
```
請使用 Python 撰寫一個互動式的正弦（sin）與餘弦（cos）波形繪圖應用程式。
要求如下：
1. **核心套件**：使用 `numpy` 進行數值運算，並使用 `matplotlib` 進行繪圖與互動控制。
2. **圖表介面**：
   - 在同一個圖表中同時繪製 y = A * sin(ω * x + φ) 和 y = A * cos(ω * x + φ) 兩條曲線。
   - 包含格線（grid）、圖表標題、X/Y 軸標籤，以及圖例（Legend）以區分 sin 和 cos。
   - X 軸範圍設定為 0 到 4π。
   - **中文字型支援**：圖表的標題、軸標籤和圖例必須使用繁體中文顯示，且程式中必須正確設定 `plt.rcParams['font.sans-serif']`（可設定支援 macOS 的 `'Arial Unicode MS'`、`'Heiti TC'` 或 Windows 的 `'Microsoft JhengHei'` 微軟正黑體），確保中文字元均能正常顯示，不會出現方塊或亂碼。
3. **互動控制（Matplotlib Widgets）**：
   - 在圖表下方加入三個滑桿（Slider），讓使用者可以即時調整以下參數：
     - **振幅 (Amplitude, A)**：調整範圍 0.1 至 5.0，預設為 1.0。
     - **頻率 (Frequency, ω)**：調整範圍 0.1 至 10.0，預設為 1.0。
     - **相位偏移 (Phase, φ)**：調整範圍 0 至 2π，預設為 0。
   - 當使用者拖曳滑桿時，波形必須即時更新。
4. **程式碼品質**：
   - 程式碼結構需清晰，並包含繁體中文註解，以利學習。
   - 確保該程式可以直接在終端機運行並顯示獨立的互動視窗。
```



### 延伸挑戰題

#### 題目1
把版面改成 2 列 2 欄(共 4 格),在四個子圖分別畫出:sin(πx)、cos(πx)、sin(2πx)、cos(2πx),每個子圖用不同顏色與線條樣式,並用 plt.title() 為每個子圖加上標題。

#### 提示

- subplot(221)、subplot(222)、subplot(223)、subplot(224) 分別對應哪一格?
- 同一個 x 陣列可以重複使用,只要換不同的 numpy 運算式即可
- 標題要在「選到該子圖之後、畫下一個子圖之前」設定





#### 畫pie


## 學生練習:半成品

請完成以下程式中標示 # 請完成 的部分。

```
# 步驟 1:匯入 matplotlib.pyplot,取別名為 plt


# 步驟 2:建立飲料名稱的串列(奶茶、紅茶、綠茶、咖啡)


# 步驟 3:建立各飲料銷售杯數的串列(50、25、35、40)


# 步驟 4:建立四種顏色的串列


# 步驟 5:建立 explode 串列,讓奶茶那一塊突出 0.2,其餘為 0


# 步驟 6:建立 figure


# 步驟 7:用 add_subplot() 建立 axes


# 步驟 8:呼叫 axes.pie(),傳入杯數,並設定 labels、colors、explode、
#         startangle=90、shadow=True、autopct='%1.1f%%'


# 步驟 9:用 plt.show() 顯示圖表

```

## 延伸挑戰題

### 題目2

讓使用者用 input() 輸入四種飲料各自的銷售杯數(原本固定的數字改成由鍵盤輸入),程式再依輸入的資料畫出圓餅圖。並自動讓「杯數最多」的那一塊突出,而不是固定突出奶茶。

### 提示:
- input() 讀進來是文字,要怎麼轉成數字才能放進 values?
- 如何找出串列中最大值的位置?(可查 max() 與 index())
- explode 串列要怎麼依「最大值位置」動態產生?

## AI賦能

```
請幫我用 Streamlit 製作一個圓餅圖（pie chart）的網頁應用程式，需求如下：

1. 使用 matplotlib 繪製圓餅圖
2. 資料內容：四個手機品牌 Nokia、Samsung、Apple、Lumia，對應數值分別為 20、30、45、10
3. 圓餅圖的顏色依序為黃色、綠色、紅色、藍色
4. 將第一塊（Nokia）使用 explode 突出顯示，突出距離 0.3
5. 加上陰影效果（shadow）
6. 每一塊要顯示百分比，格式為小數點後一位（例如 22.2%）
7. 起始角度設定為 180 度
8. 在網頁上方加一個標題文字「手機品牌市占率圓餅圖」
9. 使用 `st.pyplot()` 把圖表顯示在 Streamlit 網頁上


請給我完整的程式碼，並在最後告訴我如何用指令執行這個 Streamlit 應用程式。
```